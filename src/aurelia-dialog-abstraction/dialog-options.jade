template
  ai-dialog
    ai-dialog-header
      h2 ${title}
    ai-dialog-body
      h2(if.bind="msg") ${msg}
      compose(if.bind="viewModel", view-model.bind="viewModel")
    ai-dialog-footer
      button.btn.btn-danger(if.bind="showCancelButton", click.trigger='controller.cancel()') ${cancelBtnText}
      button.btn.btn-success(if.bind="viewModel", click.trigger='controller.ok(model)') ${okBtnText}
      button.btn.btn-success(if.bind="!viewModel", click.trigger='controller.ok()') ${okBtnText}
